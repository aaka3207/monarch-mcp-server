services:
  monarch-mcp:
    build: .
    ports:
      - "9000:9000"
    volumes:
      - monarch-data:/data
    environment:
      - MCP_TRANSPORT=sse
      - MCP_HOST=0.0.0.0
      - MCP_PORT=9000
      - MONARCH_DATA_DIR=/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; r = urllib.request.urlopen('http://localhost:9000/health'); exit(0 if r.status == 200 else 1)"]
      interval: 60s
      timeout: 15s
      retries: 3
      start_period: 10s

volumes:
  monarch-data:
